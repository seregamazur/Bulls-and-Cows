apply plugin: 'idea'
apply plugin: 'application'
apply plugin: 'java'
apply plugin: 'jacoco'

def GREEN="\033[32;1m"
mainClassName = "src.java.app.Application"

sourceSets {
    main {
        java.srcDir("src/java")
        resources.srcDir("src/resources")
    }
    test {
        java.srcDir("src/test")
    }
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

buildscript {

    repositories {
    }

    dependencies {
    }
}

jar {
    baseName = 'gs-rest-service'
    version = '0.1.0'
}

repositories {
    jcenter()
}

dependencies {
    testCompile("junit:junit:4.12")
}
test {
    afterTest { desc, result -> 
        logger.quiet "Executing test ${desc.name} [${desc.className}] with result: ${GREEN} ${result.resultType}"
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.5'
}

jacoco {
    toolVersion = "0.7.4+"
}

jacocoTestReport {
   reports {
   xml.enabled true
   csv.enabled false
   html.enabled true
   html.destination "${buildDir}/jacocoHtml"
 }
}
check.dependsOn jacocoTestReport
